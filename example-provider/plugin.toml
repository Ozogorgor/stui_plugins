[plugin]
name        = "hello-provider"
version     = "0.1.0"
type        = "metadata-provider"  # provides catalog search & trending
entrypoint  = "plugin.wasm"
description = "Minimal example provider — returns hardcoded titles, no API key needed"
abi_version = 1

[permissions]
network    = false
filesystem = []

[meta]
author   = "stui contributors"
license  = "Apache-2.0"
homepage = "https://github.com/stui/stui"
