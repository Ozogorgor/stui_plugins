[plugin]
name        = "hello-provider"
version     = "0.1.0"
type        = "metadata-provider"
entrypoint  = "plugin.wasm"
description = "Minimal example provider — returns hardcoded titles, no API key needed"

[permissions]
network = false

[meta]
author   = "stui"
license  = "Apache-2.0"
homepage = "https://github.com/Ozogorgor/stui_plugins"
